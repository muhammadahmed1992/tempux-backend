name: Deploy Dev Environment

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL_AUTH: ${{ secrets.DATABASE_URL_AUTH_DEV }}
      DATABASE_URL_PRODUCT: ${{ secrets.DATABASE_URL_PRODUCT_DEV }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: SSH Deploy to EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # --- Navigate to your project root on EC2 ---
            PROJECT_DIR="/home/ubuntu/tempux-backend"

            echo "👉 Pull latest code"

            cd "$PROJECT_DIR" || { echo "Error: Could not change to project directory. Exiting."; exit 1; }

            git fetch origin
            git checkout develop
            git pull origin develop



            echo " Install tsx "
            sudo npm install -g tsx

            export CI=true
            # --- Stop and Delete Existing PM2 Processes ---
            echo "👉 Stopping and deleting existing PM2 processes..."
            # Use 'pm2 delete all' to remove all processes managed by PM2.
            # Use '|| true' to prevent script from failing if no processes are found.
            pm2 delete all || true
            echo "PM2 processes stopped/deleted."

            echo "👉 Install dependencies for all services"
            # Navigate into each service directory, install, then go back to root
            cd "$PROJECT_DIR/api-gateway" && npm install || { echo "Error: npm install failed for api-gateway. Exiting."; exit 1; }
            cd "$PROJECT_DIR/auth-service" && npm install || { echo "Error: npm install failed for auth-service. Exiting."; exit 1; }
            cd "$PROJECT_DIR/product-service" && npm install || { echo "Error: npm install failed for product-service. Exiting."; exit 1; }
            cd "$PROJECT_DIR" # Return to project root after all installs
            # --- Inject Secrets into ecosystem.config.ts ---
            echo "👉 Injecting environment variables into ecosystem.config.ts"

            sed -i "s#INJECT_OTP_EXPIRY_DURATION#${{ secrets.OTP_EXPIRY_DURATION }}#g" ecosystem.config.ts
            sed -i "s#INJECT_MAIL_HOST#${{ secrets.MAIL_HOST }}#g" ecosystem.config.ts
            sed -i "s#INJECT_MAIL_PORT#${{ secrets.MAIL_PORT }}#g" ecosystem.config.ts
            sed -i "s#INJECT_MAIL_USERNAME#${{ secrets.MAIL_USERNAME }}#g" ecosystem.config.ts
            sed -i "s#INJECT_MAIL_PASSWORD#${{ secrets.MAIL_PASSWORD }}#g" ecosystem.config.ts
            sed -i "s#INJECT_MAIL_FROM#${{ secrets.MAIL_FROM }}#g" ecosystem.config.ts
            sed -i "s#INJECT_FRONTEND_URL#${{ secrets.FRONTEND_URL }}#g" ecosystem.config.ts
            sed -i "s#INJECT_GOOGLE_CLIENT_ID#${{ secrets.GOOGLE_CLIENT_ID }}#g" ecosystem.config.ts
            sed -i "s#INJECT_GOOGLE_CLIENT_SECRET#${{ secrets.GOOGLE_CLIENT_SECRET }}#g" ecosystem.config.ts
            sed -i "s#INJECT_GOOGLE_CALLBACK_URL#${{ secrets.GOOGLE_CALLBACK_URL }}#g" ecosystem.config.ts
            sed -i "s#INJECT_FACEBOOK_APP_ID#${{ secrets.FACEBOOK_APP_ID }}#g" ecosystem.config.ts
            sed -i "s#INJECT_FACEBOOK_APP_SECRET#${{ secrets.FACEBOOK_APP_SECRET }}#g" ecosystem.config.ts
            sed -i "s#INJECT_FACEBOOK_CALLBACK_URL#${{ secrets.FACEBOOK_CALLBACK_URL }}#g" ecosystem.config.ts
            sed -i "s#INJECT_EMAIL_ENCRYPTION_KEY#${{ secrets.EMAIL_ENCRYPTION_KEY }}#g" ecosystem.conINJECT_fig.ts
            sed -i "s#INJECT_SALT_ROUND#${{ secrets.SALT_ROUND }}#g" ecosystem.config.tsINJECT_
            sed -i "s#INJECT_PORT#${{ secrets.AUTH_PORT }}#g" ecosystem.config.tsINJECT_
            sed -i "s#INJECT_PORT#${{ secrets.AUTH_PORT }}#g" ecosystem.config.tsINJECT_
            sed -i "s#INJECT_USER_SERVICE_URL#${{ secrets.USER_SERVICE_URL }}#g" ecosystem.config.tsINJECT_
            sed -i "s#INJECT_AUTH_SERVICE_BASE_URL#${{ secrets.AUTH_SERVICE_BASE_URL }}#g" ecosystem.config.ts
            sed -i "s#INJECT_PRODUCT_SERVICE_BASE_URL#${{ secrets.PRODUCT_SERVICE_BASE_URL }}#g" ecosystem.config.ts

            echo "👉 Run Prisma migrate + seed for auth-service"
            cd "$PROJECT_DIR/auth-service" || { echo "Error: Could not change to auth-service directory. Exiting."; exit 1; }
            # For Prisma commands, you still need to pass the DB URL directly as an env var
            # because PM2 hasn't reloaded the ecosystem file yet for the running processes.
            # adding temporarily.
            echo ${{ secrets.DATABASE_URL_DEV_USERS }}
            echo ${{ secrets.DATABASE_URL_DEV_PRODUCT }}
            DATABASE_URL_DEV_USERS="${{ secrets.DATABASE_URL_DEV_USERS }}" npx prisma migrate deploy --schema prisma/schema.prisma || { echo "Error: Prisma migrate deploy failed for auth-service. Exiting."; exit 1; }
            DATABASE_URL_DEV_USERS="${{ secrets.DATABASE_URL_DEV_USERS }}" npx prisma db seed --schema prisma/schema.prisma || { echo "Error: Prisma db seed failed for auth-service. Exiting."; exit 1; }
            cd "$PROJECT_DIR" # Return to project root


            echo "👉 Run Prisma migrate + seed for product-service"
            cd "$PROJECT_DIR/product-service" || { echo "Error: Could not change to product-service directory. Exiting."; exit 1; }
            # For Prisma commands, you still need to pass the DB URL directly as an env var
            # because PM2 hasn't reloaded the ecosystem file yet for the running processes.
            DATABASE_URL_DEV_PRODUCT="${{ secrets.DATABASE_URL_DEV_PRODUCT }}" npx prisma migrate deploy --schema prisma/schema.prisma || { echo "Error: Prisma migrate deploy failed for auth-service. Exiting."; exit 1; }
            DATABASE_URL_DEV_PRODUCT="${{ secrets.DATABASE_URL_DEV_PRODUCT }}" npx prisma db seed --schema prisma/schema.prisma || { echo "Error: Prisma db seed failed for auth-service. Exiting."; exit 1; }
            cd "$PROJECT_DIR" # Return to project root

            echo "👉 Start/Reload PM2 processes" # Changed message
            # Use 'pm2 start' which handles both initial start and updates/reloads.
            # '--update-env' ensures new environment variables from sed are picked up.
            pm2 start ecosystem.config.ts --interpreter tsx --update-env || { echo "Error: PM2 start/reload failed. Exiting."; exit 1; }
